# Apache Doris FE 自定义配置
# 此文件挂载到 /opt/apache-doris/fe/conf/fe_custom.conf

# 启用多 Catalog 支持（用于 Iceberg、Hive 等外部数据源）
enable_multi_catalog = true

# 优先级网络配置
priority_networks = 172.30.0.0/24

# 可以根据需要添加其他配置项
# 例如：
# log_level = INFO
# sys_log_level = INFO
