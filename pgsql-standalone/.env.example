COMPOSE_PROJECT_NAME=pgsql-standalone
POSTGRES_IMAGE=postgres:16-alpine
TZ=UTC

# 宿主机映射端口（容器内固定 5432）
PG_PORT=5432

# 数据库账户与库名（首次初始化卷时生效；已有数据卷不会改用户/库）
POSTGRES_USER=postgres
POSTGRES_DB=postgres
# 务必改成强密码，勿提交 .env
POSTGRES_PASSWORD=请改成强密码
