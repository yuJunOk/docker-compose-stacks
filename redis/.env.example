COMPOSE_PROJECT_NAME=redis

# 宿主机监听地址：默认 127.0.0.1；需局域网访问时可改为 0.0.0.0
REDIS_BIND=127.0.0.1

# 宿主机映射端口（容器内固定 6379）
REDIS_HOST_PORT=6379

# 用户名：默认 default（与 requirepass 行为一致）；改为其它值则启用 ACL 专用用户并关闭 default
REDIS_USERNAME=default

# 必填
REDIS_PASSWORD=请改为强密码
