COMPOSE_PROJECT_NAME=hermes
HERMES_AGENT_IMAGE=nousresearch/hermes-agent:latest
TZ=UTC
HERMES_UID=1000
HERMES_GID=1000

# 第三方 / OpenAI 兼容 API 的 Bearer 密钥（≥8 位，勿泄露）
API_SERVER_KEY=请改成强随机串
# 浏览器直连 API 时的 CORS，* 最宽；可改为 http://你的域名:端口
API_SERVER_CORS_ORIGINS=*
